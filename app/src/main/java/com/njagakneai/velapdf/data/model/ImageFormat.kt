package com.njagakneai.velapdf.data.model

enum class ImageFormat(val extension: String, val displayName: String) {
    JPG("jpg", "JPG / JPEG"),
    PNG("png", "PNG (Lossless)"),
    WEBP("webp", "WebP (Modern)")
}
